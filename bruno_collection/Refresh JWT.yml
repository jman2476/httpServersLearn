info:
  name: Refresh JWT
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:8080/api/refresh
  headers:
    - name: Authorization
      value: ""
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
