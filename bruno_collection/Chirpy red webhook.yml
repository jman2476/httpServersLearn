info:
  name: Chirpy red webhook
  type: http
  seq: 16

http:
  method: GET
  url: http://localhost:8080/api/polka/webhooks
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
