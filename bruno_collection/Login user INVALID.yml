info:
  name: Login user INVALID
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/api/login
  body:
    type: json
    data: |-
      {
        "email": "bobthomas@tankengine.com",
        "password": "honkmyhornyhorn"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
