info:
  name: New user 2
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8080/api/users
  body:
    type: json
    data: |-
      {
        "email": "bankter@tankengine.com",
        "password": "tootToot"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
