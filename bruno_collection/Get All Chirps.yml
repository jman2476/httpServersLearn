info:
  name: Get All Chirps
  type: http
  seq: 13

http:
  method: GET
  url: http://localhost:8080/api/chirps
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
