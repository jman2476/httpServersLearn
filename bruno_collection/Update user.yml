info:
  name: Update user
  type: http
  seq: 7

http:
  method: PUT
  url: http://localhost:8080/api/users
  body:
    type: json
    data: |-
      {
        "email": "dankthomas@tankengine.com",
        "password": "puffPuff"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
